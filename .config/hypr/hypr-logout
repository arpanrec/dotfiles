#!/usr/bin/env bash

# Lock first (optional but recommended)
if command -v hyprlock >/dev/null; then
    hyprlock
    sleep 0.5
fi

# Terminate the current systemd session
loginctl terminate-session "$XDG_SESSION_ID"
