---@module 'hl'

local my_programs = require("lib.my_programs")

local env = require("lib.env")

local execs = require("lib.execs")

local look_and_feel = require("lib.look_and_feel")

local keybinds = require("lib.keybinds")

local windows_and_workspaces = require("lib.windows_and_workspaces")

local input = require("lib.input")

local monitors = require("lib.monitors")

local permissions = require("lib.permissions")
